import React from 'react';
import ReactDOM from 'react-dom/client';
import TokenCreatorApp from './TokenCreatorApp';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <TokenCreatorApp />
  </React.StrictMode>
);

